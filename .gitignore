build/
.dart_tool/
*.apk
.idea/
.flutter-plugins-dependencies
android/.kotlin/
flutter_*.log

# Flutter web build output only — keep /web/ sources in git for Vercel
build/web/

# Huge web duplicates — production loads GLB/reels from Cloudflare R2
/web/*.glb
/web/reels_videos/
/web/models/
/web/landing page product/kurta/
/web/landing page product/shalwar kameez/

# Optional: keep huge 4K reels only on your machine (production uses R2 CDN)
/assets/reels_videos/*.mp4
